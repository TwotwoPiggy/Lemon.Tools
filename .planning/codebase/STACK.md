# Tech Stack

## Core
- **Platform**: .NET
- **Language**: C# (versions 8 through 10+)
- **Target Frameworks**: Mix of `.net10.0`, `.netstandard2.1`, `.netstandard2.0` and potentially `.NET Framework` for WinForms/WPF depending on UI usage.

## Libraries & Frameworks
- `Microsoft.Extensions.DependencyInjection`
- `Microsoft.Extensions.Configuration`
- `Newtonsoft.Json` (JSON serialization)

## Tooling
- Visual Studio / MSBuild
- NuGet (custom NuGet package generation enabled)
- SQLite (database)
